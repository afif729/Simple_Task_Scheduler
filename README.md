# Simple_Task_Scheduler
A simple yet effective task scheduler that schedules the tasks entered by the users and guides them in ordering the tasks. This python program has been trained on a generated data-set. Users can input necessary details like no of tasks, priority of tasks and deadline, and the system schedules and orders them.
